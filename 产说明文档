拾词 - 产品功能说明文档

一、产品概述

产品名称：拾词
产品定位：Chrome 浏览器扩展（Manifest V3），一键提取网页英文单词，用于词汇背诵
目标用户：使用「不背单词」等背词 APP 的学习者
核心价值：从任意网页快速提取英文单词，自动过滤已学词汇，支持批量导入背词 APP，激励持续学习习惯

---
二、核心功能模块

1. 单词提取功能（popup.js）

功能描述：从当前浏览的网页中智能提取英文单词

具体能力：

1. 智能文本提取
  - 使用 TreeWalker 遍历页面所有可见文本节点
  - 自动排除 script、style、noscript、iframe、svg、path、code、pre 等非内容标签
  - 过滤隐藏元素（display:none、visibility:hidden）
  - 提取页面完整可见文本内容
2. 单词匹配规则
  - 正则匹配：/[a-zA-Z]{3,}/g（3 字符以上的英文单词）
  - 自动去重（使用 Set 数据结构）
  - 自动转小写统一格式
  - 按字母顺序排序输出
3. 智能过滤（已收录词汇）
  - 提取时自动排除用户已收录的词汇
  - 只显示未学习的新单词
  - 显示统计卡片：「新词」数量 + 「词袋已有」数量
  - 全部已收录时提示「当前页面单词均已收录」
4. 提取结果展示
  - 预览前 30 个单词（超出滚动查看）
  - 每个单词以标签形式展示
  - 按字母序排列
5. 页面兼容性提示
  - 自动检测 chrome://、chrome-extension:// 等特殊页面
  - 特殊页面显示「此页面不支持提取」提示
  - 连接失败时显示刷新重试提示

---
2. 导出功能（popup.js）

功能描述：将提取的单词导出用于背词 APP 导入

具体能力：

1. 复制并加入词袋
  - 一键复制全部单词到剪贴板
  - 格式：每行一个单词（换行分隔）
  - 复制成功后按钮显示「已复制 ✓」反馈（1.5秒）
  - 自动收录：复制时自动将单词加入「我的词袋」
2. 下载 TXT
  - 下载为纯文本文件
  - 文件名格式：words_YYYY-MM-DD.txt（带日期）
  - 内容格式：每行一个单词
  - 自动收录：下载时自动将单词加入「我的词袋」
  - 下载并入袋成功提示

---
3. 我的词袋管理（options）

功能描述：管理用户已学习的词汇库，用于提取时自动过滤，同时展示学习统计

具体能力：

1. 学习统计展示
  - 已拾起词汇总数（大字展示）
  - 连续天数徽章（火焰图标 + 连续 X 天）
    - 首次添加词汇开始计数
    - 每日添加词汇时自动更新
    - 连续使用累加，中断后重新从 1 开始
    - 新安装显示「连续 0 天」
  - 本周新增数量（动态统计，每周自动重置）
    - 周一开始归零重新计数
    - 删除本周添加的词汇时同步减少
  - 进度条（目标 1800 词）
  - 备份提醒（≥100 词时显示）

2. 词汇存储结构
  - 使用 chrome.storage.local 本地持久化存储
  - 存储格式：[{ word: 'xxx', addedAt: 'YYYY-MM-DD' }, ...]
  - 存储键名：collectedVocabulary
  - 兼容旧数据格式（纯字符串数组自动升级）
  - 支持记录每个词汇的首次添加时间

3. 快速添加词汇
  - 输入框支持单个或批量粘贴
  - 支持多种分隔符：换行、空格、逗号、分号、制表符
  - Enter 或点击按钮快速入袋
  - 显示入袋成功数量或「词汇已存在」

4. 批量导入
  - 上传 TXT 文件按钮（SVG upload 图标）
  - 点击直接打开文件选择窗口
  - 自动解析文件内容并过滤无效格式
  - 显示导入成功数量

5. 词汇列表管理
  - 显示最近 100 个词汇（倒序）
  - 超出 200px 高度滚动查看
  - 每个词汇标签带删除按钮
  - 删除本周词汇时同步减少本周统计
  - 清空全部功能（带二次确认）

6. 导出备份
  - 导出为 TXT 文件
  - 文件名格式：vocab_backup_YYYY-MM-DD.txt
  - 内容按字母顺序排序
  - 用于数据备份或迁移

7. 返回提取入口
  - 顶部右侧「返回拾起」按钮（箭头图标）
  - 点击跳转到 popup 主界面

---
4. 连续天数追踪（streak）

功能描述：激励用户持续学习习惯

具体能力：

1. 数据存储
  - 存储键名：streakData
  - 格式：{ lastDate: 'YYYY-MM-DD', streakDays: number }
2. 更新逻辑
  - 首次添加词汇：streak = 1
  - 今天已记录：不增加（防止多次操作累加）
  - 昨天记录：streak++
  - 中断多天：重置为 1（重新开始）
3. 显示规则
  - 徽章始终显示（含连续 0 天）
  - 新安装用户：显示「连续 0 天」，添加词汇后变为「连续 1 天」

---
5. 本周统计（weekly）

功能描述：动态展示本周学习成果，每周自动重置

具体能力：

1. 数据存储
  - 存储键名：weeklyData
  - 格式：{ weekStart: 'YYYY-MM-DD（周一）', weeklyCount: number }
2. 计算逻辑
  - 周一自动重置计数
  - 添加词汇时累加
  - 删除词汇时检查 addedAt：
    - 若 addedAt >= 本周周一，则减少本周计数
    - 若非本周添加，不影响本周统计
3. 显示格式
  - 进度条下方显示「本周新增 +X」

---
6. 用户界面功能

主界面（popup.html）：

- 品牌 Logo（SVG sparkles 图标）
- 标题：拾起这页的 生词
- 副标题：从当前网页智能挑出你还不会的词
- 「拾起本页生词」按钮（渐变背景 + SVG sparkles）
- 统计卡片：「新词」 + 「词袋已有」
- 单词预览区域（前 30 个，超出滚动）
- 「复制并加入词袋」+「下载 TXT」按钮
- 底部：「自动入袋 · 下次不再拾取」 + 「我的词袋」入口

管理界面（options.html）：

- 品牌 Logo（SVG library 图标） + 「我的词袋」
- 返回按钮（SVG arrow-left + 「返回拾起」）
- 统计区：词汇总数 + 连续天数徽章（SVG flame）
- 进度条 + 本周新增 + 目标提示
- 输入框（支持批量粘贴） + 添加按钮 + 上传按钮（SVG upload）
- 词汇列表（最近拾起，滚动展示）
- 备份提醒（≥100 词）+ 导出链接
- 清空按钮

消息提示系统：

- 成功（绿色渐变）、错误（红色渐变）、信息（玫瑰粉渐变）
- 居中/顶部显示（popup 顶部，options 居中）
- 自动消失（默认 2秒，特殊 3秒）

---
7. 扩展配置

基本信息：

- 扩展名称：拾词
- 版本：1.2.0
- 描述：一键提取网页英文单词，用于词汇背诵

权限声明：

- activeTab：访问当前标签页
- scripting：动态注入脚本执行提取
- storage：存储词汇数据和统计信息

页面配置：

- popup.html：主界面弹窗
- options.html：我的词袋管理页面（options_page）

图标：

- 16×16、48×48、128×128 三种尺寸
- 存储路径：src/icons/iconXX.png

---
三、功能流程说明

单词提取流程

用户点击「拾起本页生词」
  → 检查页面兼容性
  → 注入脚本到页面
  → TreeWalker 遍历文本节点
  → 正则匹配英文单词
  → 去重 + 小写 + 排序
  → 加载已收录词汇（带时间戳）
  → 过滤已收录单词
  → 显示「新词」+「词袋已有」统计
  → 用户选择导出方式
  → 自动收录到词袋（记录时间戳）
  → 更新连续天数 + 本周统计

词汇管理流程

用户进入「我的词袋」
  → 加载词汇库 + 统计数据
  → 显示总数 + 连续天数 + 本周新增 + 列表
  → 用户操作（添加/上传/删除/清空）
  → 更新存储（含时间戳）
  → 同步更新统计（连续天数/本周）
  → 刷新显示

---
四、技术特性

1. 无构建系统：源码直接作为扩展运行，无需编译打包
2. Manifest V3：符合最新 Chrome 扩展标准
3. 动态脚本注入：使用 chrome.scripting API，无需 content script 常驻
4. 本地存储：数据存储在浏览器本地，卸载扩展后数据丢失
5. 智能过滤：已收录词汇自动过滤，避免重复学习
6. 时间戳存储：记录添加时间，支持精细化统计
7. 连续天数追踪：激励持续学习习惯
8. 本周动态统计：展示本周成果，每周自动重置
9. SVG 图标：所有图标使用 SVG，视觉统一
10. 批量操作：支持批量粘贴、批量导入、批量收录

---
五、使用场景

1. 阅读英文文章：一键提取文章所有生词
2. 学习网页内容：从教程、文档提取专业词汇
3. 批量导入背词 APP：导出后导入「不背单词」等应用
4. 词汇积累管理：长期维护已学习词汇库
5. 学习习惯追踪：连续天数激励每日学习
6. 本周成果查看：动态统计本周新增词汇
7. 数据备份迁移：导出备份防止数据丢失

---
六、数据存储结构

| 存储键 | 格式 | 说明 |
|--------|------|------|
| collectedVocabulary | [{ word, addedAt }] | 词汇列表 + 添加时间 |
| streakData | { lastDate, streakDays } | 连续天数数据 |
| weeklyData | { weekStart, weeklyCount } | 本周统计数据 |

---
七、UI 设计风格

- 温暖奶油风格（cream tones）
- 玫瑰粉为主色调（rose-500）
- 玻璃态效果（backdrop-blur）
- 渐变按钮和卡片
- Lucide SVG 图标库
- 圆角设计（16-28px）